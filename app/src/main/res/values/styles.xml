<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="pantalla_inicio" parent="AppTheme">
        <item name="android:colorBackground">@color/grisFondo</item>
    </style>

    <style name="formulario">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@drawable/wall_background</item>
    </style>

    <style name="fuente">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/fuenteNaranja</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColorHint">@color/fuenteNaranja</item>
    </style>

    <style name="tabla">
        <item name="android:layout_marginLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_marginRight">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_marginTop">@dimen/activity_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/activity_vertical_margin</item>
        <item name="android:color">@color/black</item>
    </style>

    <style name="filasTabla">
        <item name="android:layout_margin">@dimen/margin_tabla</item>
        <item name="android:background">@color/materialgray</item>
    </style>

    <style name="elementoTabla" parent="fuente">
        <item name="android:layout_margin">@dimen/margin_tabla</item>
    </style>

    <style name="elementoTabla_icono" parent="elementoTabla">
        <item name="android:drawableRight">@drawable/money_icon</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
